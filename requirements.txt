# Use Python 3.11 or 3.12 (not 3.14 - it breaks these libraries).
# Install with:  pip install -r requirements.txt

# Core: needed for demo.py, segmentation, and the pipeline
torch>=2.2
transformers>=4.40
numpy>=1.24
nltk>=3.8

# ASR-error analysis (word error rate)
jiwer>=3.0

# Result figures
matplotlib>=3.7

# Summarization fine-tuning and ROUGE evaluation
sentencepiece>=0.2
rouge-score>=0.1.2
accelerate>=0.30

# Speech-to-text: only needed to re-run Whisper transcription (not for the demo).
# datasets is pinned <3.0 on purpose - 3.x pulls in torchcodec/FFmpeg and breaks.
faster-whisper>=1.0
datasets<3.0
soundfile>=0.12
